# 🚀 部署与自动更新说明（GitHub 网页 + 电脑每 4 小时自动更新）

## 为什么这样分工？

- **网站托管在 GitHub Pages**：公开网址，**任何时间、任何网络**都能打开（手机/电脑均可）。
- **为什么数据要用你的电脑定时更新？** 竞彩数据来自中国体彩官网，该官网**封锁境外 IP**
  （美国/海外访问会返回 HTTP 567），GitHub 的云端机器抓不到。
  所以：**网页由云端承载，数据由你的电脑每 4 小时抓取一次并推送**到 GitHub，网页随之刷新。

> 只需电脑开机即可（每次运行几秒钟）。电脑关机期间网页仍能打开，显示最近一次的数据。

---

## 一、一次性的设置（只需一次）

### 1) 网页数据源设置（重要，改一次）
浏览器打开仓库页面 → **Settings → Pages**：
- **Build and deployment / Source**：选 **Deploy from a branch**
- **Branch**：`main`；**folder**：`/docs`
- 点 **Save**

完成后网址即生效：**https://wanglin-777.github.io/jc-football/**
（每次推送代码/数据后，GitHub 会自动用 `docs/` 重建网页，约 1~3 分钟生效）

### 2) 电脑安装"每 4 小时自动更新"
在 `d:\python\jc_football\` 双击 **`安装自动更新.bat`**
（它会注册一个 Windows 计划任务：每 4 小时静默执行“抓体彩数据→重建网页→推送到 GitHub”）

> 取消自动更新：双击 `卸载自动更新.bat`
> 想立刻更新一次：双击 **`立即更新网页.bat`**（或命令行运行 `refresh_push.py`）

---

## 二、日常使用

1. 打开网址 **https://wanglin-777.github.io/jc-football/**（建议收藏）
2. 网页顶部显示**数据更新时间与数据来源**，据此判断是否最新
3. 电脑开着时：每 4 小时自动更新；也可随时双击 `立即更新网页.bat` 手动刷新

---

## 三、项目里与自动更新相关的文件

| 文件 | 作用 |
|------|------|
| `refresh_push.py` | 抓最新数据 → 重建 `docs/` 网页 → git 提交推送（核心） |
| `安装自动更新.bat` / `卸载自动更新.bat` | 注册/删除“每 4 小时”Windows 计划任务 |
| `立即更新网页.bat` | 手动立即更新一次并打开网页 |
| `docs/` | 生成的静态网页（推送到 GitHub 后由 Pages 发布） |
| `build_site.py` | 抓数据+建模 → 生成 `docs/index.html` |

---

## 四、常见问题

| 现象 | 处理 |
|------|------|
| 网页数据是旧的 | 电脑上双击 `立即更新网页.bat`，或等下一次 4 小时自动更新 |
| 电脑没开机 | 网页仍能打开，但会显示最近一次抓取的数据 |
| 首次部署看不到内容 | 确认 Settings→Pages 已设为 `main` + `/docs`，等 1~3 分钟 |
| 网页打不开 | 网址拼写检查；GitHub Pages 是公开网站，无需任何额外操作 |

---

## 五、本机临时预览（可选）
双击 **`生成网站.bat`** 可在本地浏览器预览最新网页效果（不影响线上）。
