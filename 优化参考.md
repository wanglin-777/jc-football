# 本轮优化参考（2026-09-08）

本轮阅读公开项目与作者文档，借鉴方法与信息组织，未复制第三方项目代码或引入其依赖。

| 参考 | 值得借鉴的内容 | 本项目的应用 |
| --- | --- | --- |
| [Penaltyblog 模型文档](https://docs.pena.lt/y/models/dixon_coles.html)与[指标文档](https://docs.pena.lt/y/metrics/index.html) | 比分概率建模、低比分相关性修正、概率质量指标 | 保留现有模型，完善 Brier/对数损失与市场基线对照；Dixon–Coles 暂不默认启用 |
| [Worldcup Forecaster](https://github.com/VirajMishra1/worldcup-forecaster) | 作者描述了赛前公布概率、赛后核验的预测账本流程 | 保存预测时间、开赛时间、特征和市场概率；开赛后不覆盖已存档预测 |
| [Football Analytics Dashboard](https://github.com/itzmore-mph/football-analytics-dashboard) | 足球分析的多标签视图与统计可视化 | 保留原串关首屏，概率表增加概率条、可展开依据和筛选 |
| [Soccerdata](https://github.com/probberechts/soccerdata) | 多足球数据源的统一访问 | 本轮先明确缓存/联网边界，保持现有数据源，避免新增抓取依赖 |

## 为什么先改数据流程

原有 `force=False` 仍会走联网分支；离线页面生成还会抓取赛果、调用 AI 和写预测账本。
这些行为已隔离。测试会检查联网函数和写入函数没有被调用。

原有联赛进球均值只以今天为截止点，重算过去比赛时可能用到未来结果。现在同近期战绩一样，
以待预测比赛的日期为边界，排除当天及以后赛果。另外修复了从客队视角看交锋时将平局记作胜的问题。

新账本保存模型版本和输入特征，但历史旧文件缺少这些字段；无法据此恢复严格的历史时间外回测。
本轮没有用修改后的模型重写旧预测，现有评估数字仍然属于旧存档。

## 已完成的历史子模型实验

新攻防算法根据总体与主客场样本量向联赛均值收缩。2,625 场满足两队至少 6 场历史的样本，
按日期分为历史积累期、621 场验证期和 353 场测试期。验证期选择收缩参数 12。
测试期首选命中率 47.59% → 49.58%，Brier 0.6302 → 0.6186，对数损失 1.0520 → 1.0352。
按日期成组重采样 1,000 次，命中率差 95% 区间约 -1.79～+5.44 个百分点，仍包含零。
因此只能报告当前测试集上的改善，不能承诺持续提高命中率。

报告及输入缓存 SHA-256 在 `data/backtest_report.json`，代码为 `backtest.py`。
没有历史完整赔率，所以本实验只比较历史攻防子模型。候选模型已用于有历史支持的胜平负分支，
保留原算法开关和同场旧算法影子预测；赔率分支和独立总进球玩法仍沿用原有方法。

## 下一轮模型验证路线

1. 积累新赛前快照以及对应的最终赛果，按时间划分训练期、验证期与测试期。
2. 用相同比赛、相同赔率时点比较纯市场、当前泊松融合模型与候选 Dixon–Coles 模型。
3. 只在训练期拟合攻防强度与低比分相关参数，验证期选择参数，最后在保留测试期报告结果。
4. 同时观察 Brier、对数损失、首选命中率和样本数。不要把调参所用样本的成绩当作泛化能力。

当前新界面由 `dashboard.py`、`ui/dashboard.css` 和 `ui/dashboard.js` 维护，
在生成时嵌入单文件 HTML，兼容原有静态托管与直接打开方式。
